# INSTRUCCIÓN: Cargar dotenv en desarrollo local
# Fecha: 16/05/2026
# Sprint: setup local funcional (pre-demo Enerlight)
# Branch: desarrollo


## CONTEXTO

Sergio nunca ha levantado servidor local. Hasta hoy probaba solo en Railway.
Para correr el smoke manual de la checklist previa al push, necesita entorno local funcional.

Estado actual:
- `dotenv@17.4.2` ya instalado vía `npm install dotenv --save` (cambios en `package.json` + `package-lock.json` sin commitear)
- `.env` ya creado en raíz, encoding UTF-8 sin BOM verificado (Length 1377, primeros bytes `23 20 3D 3D`)
- `.env` ya está en `.gitignore` (junto con `.env.local` y `.env.production`)
- 6 vars críticas llenadas manualmente con valores reales copiados de Railway (Sergio confirma)

5 commits del sprint pendientes de push (no tocar):
- ebbc58b feat(alertas): soporte deep-link desde dashboard
- 2e2e71d feat(metricas): nueva vista para superadmin con KPIs operativos y financieros
- 04cab8b docs(arch): anotar decision Zona 2 + normalizar line endings de ARCHITECTURE.md
- 3faa4a5 feat(dashboard): rediseno mission control supervisor SST
- fd1d520 feat(router): soporte querystring en hash


## TAREA

Editar `index.js` para que cargue dotenv SOLO en desarrollo local.
En Railway las variables ya están inyectadas, así que no debe ejecutarse ahí.


## CAMBIO EXACTO

Archivo: `index.js`

Insertar el siguiente bloque INMEDIATAMENTE DESPUÉS del header de comentarios
(después de la línea 5 con `// ═══...`) y ANTES del primer `require` (línea 6 actual: `const express = require('express')`).

```javascript

// Cargar variables de entorno desde .env SOLO en desarrollo local.
// En Railway (produccion) las variables ya estan inyectadas en el runtime.
if (process.env.NODE_ENV !== 'production') {
  require('dotenv').config();
}

```

Resultado esperado (primeras 15 líneas del archivo):

```javascript
// ═══════════════════════════════════════════════════════════════════
// index.js
// Punto de entrada — Express, canales y cron de alertas
// CERO — Sistema de gestión de operaciones de campo
// ═══════════════════════════════════════════════════════════════════

// Cargar variables de entorno desde .env SOLO en desarrollo local.
// En Railway (produccion) las variables ya estan inyectadas en el runtime.
if (process.env.NODE_ENV !== 'production') {
  require('dotenv').config();
}

const express = require('express');
const helmet = require('helmet');
const { registrarCanalWhatsapp } = require('./canales/whatsapp');
```


## REGLAS CRÍTICAS

1. NO TOCAR los caracteres `═══` del header. Aunque en terminal Windows se vean como `â•â•`, en el archivo real están correctos en UTF-8.

2. Guardar `index.js` en UTF-8 SIN BOM. Si Cursor sugiere otro encoding, forzar UTF-8 sin BOM.

3. NO modificar ninguna otra línea del archivo. Solo insertar el bloque dotenv en la posición indicada.

4. NO hacer push. Sergio confirma cuando pase el smoke manual.


## VERIFICACIONES POST-CAMBIO

Ejecutar en orden y reportar resultado de cada uno:

1. Sintaxis OK:
   ```
   node --check index.js
   ```

2. Tests siguen verde (no debería bajar de 101):
   ```
   npm test
   ```

3. Servidor arranca local sin crash:
   ```
   npm start
   ```
   Esperado: log "Servidor escuchando en puerto 8080" (o similar) sin errores rojos.
   Si crashea por variable faltante, reportar QUÉ variable falta — Sergio la agrega al `.env`.

4. Conexión a Supabase responde:
   En otra terminal: `curl http://localhost:8080/api/dashboard/resumen` o equivalente que toque BD.
   Esperado: 200 OK o 401 (sin login). NO debe ser 500 ni crash del servidor.


## COMMITS (orden obligatorio, NO mezclar)

Tres commits separados. Una intención = un commit.

### Commit 1 — dependencia
```
git add package.json package-lock.json
git commit -m "chore(deps): instalar dotenv para carga local de variables de entorno"
```

### Commit 2 — código
```
git add index.js
git commit -m "chore(local): cargar dotenv solo en desarrollo

Permite levantar servidor local con variables desde .env.
En Railway (NODE_ENV=production) no se ejecuta para evitar
sobrescribir variables ya inyectadas en el runtime."
```

### Commit 3 — instrucción (este archivo)
```
git add instrucciones/setup-local-dotenv-v1.txt
git commit -m "docs(instr): instruccion setup local con dotenv"
```


## PENDIENTE PARA SERGIO (no para Claude Code)

Después de los 3 commits y antes del push:

1. Smoke manual de la checklist (la que ya se entregó arriba en el chat)
2. Confirmar OK
3. PUSH: `git push origin desarrollo` (deja la rama 8 ahead → 0 ahead)


## ROLLBACK SI ALGO FALLA

```
git reset --soft HEAD~3   # vuelve los 3 commits a staging sin perderlos
git restore --staged .
git restore index.js
```

NO usar `git reset --hard` — perderías el `.env` y la instalación de dotenv.
